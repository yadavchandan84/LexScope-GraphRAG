FROM nginx:alpine

# Copy the custom nginx configuration
COPY nginx.conf /etc/nginx/conf.d/default.conf

# Copy static frontend files to the web server root
COPY frontend/ /usr/share/nginx/html/

EXPOSE 5500

CMD ["nginx", "-g", "daemon off;"]
